import { FC } from "react";

import { Layout } from "components";

const Homepage: FC = () => {
  return (
    <Layout title="Home">
      <div className="font-medi">Homepage</div>
    </Layout>
  );
};

export default Homepage;
